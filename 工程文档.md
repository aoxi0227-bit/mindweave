# 思脉 MindWeave · 工程文档

> 面向运行、扩展、验证与部署的工程手册。架构原理见《技术文档.md》。

---

## 1. 环境要求

| 项 | 要求 | 说明 |
|----|------|------|
| 浏览器 | Chrome / Edge / Safari 近两版 | 用到 `color-mix`、`ReadableStream`、SVG CSS transform |
| Node.js | ≥ 18 | 仅 `server.js` 需要；**只用标准库，无需 `npm install`** |
| 真实模型（可选） | 本机已装并登录 `claude` CLI | 桥接读取其 `~/.claude/settings.json` |

> 仅用 Mock 演示时，**连 Node 都不需要**，双击 html 即可。

---

## 2. 快速开始

### 2.1 零配置（Mock 演示）
直接双击 `mindweave.html`，或 `open mindweave.html`。默认载入《雾港孤灯》大纲，可与内置脚本对话改图。

### 2.2 接入本地 Claude CLI（推荐，无 mock）
```bash
cd /Users/Sam/文档/mindmap/Kimi_Agent_思维导图双向规划
node server.js
```
终端打印形如：
```
[mindweave-bridge] listening on http://127.0.0.1:4317
[mindweave-bridge] claude CLI: 2.1.211 (Claude Code) @ /Users/Sam/.local/bin/claude
[mindweave-bridge] proxy: http://127.0.0.1:15721 · model=MiniMax-M3
[mindweave-bridge] ready=true (open the URL above; mock is NOT used when ready)
```
**用浏览器打开它打印的地址**（`http://127.0.0.1:4317`，不是 file://）。页面首屏自动识别并切到「本地 Claude CLI」模式，模型芯片显示「本地 Claude · <model>」。此时对话走真实模型，**不使用 mock**。

切换端口：`PORT=5050 node server.js`。


### 2.4 一键启动 / 关页自停（macOS）
- 双击 `思脉MindWeave.app` 或 `启动思脉.sh`：无终端静默起 `server.js` + 自动开浏览器；单实例锁防重复。
- **关页自停**：页面在 CLI 模式下每 2.5s `POST /api/ping`，加载时 `/api/hello`、卸载时 `sendBeacon /api/bye`；后台维护客户端计数，归零 7s 后自退，心跳 18s 超时兜底崩溃场景。
- 相关端点：`/api/hello` `/api/ping` `/api/bye`；进程文件 `.server.pid`/`.server.lock`（已 git 忽略）。
- 首次双击被 Gatekeeper 拦截：右键→打开，或 `xattr -dr com.apple.quarantine 思脉MindWeave.app`。

### 2.3 用云端 / 其它本地模型
设置（齿轮）→「自定义 API / 本地模型」→ 选预设或填 `Base URL / Key / Model` → 测试连接 → 保存。本地 Ollama/LM Studio 可点「重新探测」一键接入。

---

## 3. 项目结构
```
mindweave.html                        前端单文件（HTML+CSS+JS）
server.js                             本地桥接（识别+代理流式+静态托管）
技术文档.md                            架构/算法/协议
工程文档.md                            本文
思维导图AI对话网页-工程规划文档.md       原始需求与决策记录
README.md                             概览
.gitignore
demo/                                 验证截图（git 忽略）
app/                                  旧 Vite 脚手架（未启用，git 忽略）
```

---

## 4. 配置项

### 4.1 前端（localStorage）
见技术文档 §2.2。`settings.mode ∈ {mock, api, cli}`；`ui.modeExplicit` 标记用户是否手动选过模式（未手动选且桥接可用时，首屏自动置 `cli`）。

### 4.2 桥接（server.js）
| 来源 | 作用 |
|------|------|
| `PORT`（env，默认 4317） | 监听端口 |
| `~/.claude/settings.json → env.ANTHROPIC_BASE_URL` | 上游代理地址 |
| `~/.claude/settings.json → env.ANTHROPIC_API_KEY` | 上游鉴权（代理托管值，原样转发） |
| `/v1/models` | 自动选取默认模型 |

`/api/health?force=1` 强制重探（设置面板「重新识别」）。

---

## 5. 扩展指南

- **新增场景模板**：`mindweave.html` 的 `TEMPLATES` 数组加一项 `{id,name,icon,color,group,desc,md,sugg[]}`。
- **新增预设服务**：设置面板 `<select id="presetSelect">` 加 `<option value="BASE_URL">`。
- **新增内置导图主题**：`MAP_THEMES` 加 `{id,name,branches[5]}`；运行时自建主题走 `state.customThemes`（持久化）。
- **改默认 System Prompt**：常量 `SYSPROMPT`（同时影响 api 与 cli 模式，经 `buildApiMessages` 注入 `system`）。
- **桥接换上游**：改 `~/.claude/settings.json` 的 `ANTHROPIC_BASE_URL` 后，前端点「重新识别」或重启 server。

---

## 6. 验证清单

> 已用真实浏览器（ego-browser）+ DOM 冒烟 + 逻辑单测逐项验证。

- [x] 首屏渲染 11 节点大纲；MD↔导图双向同步
- [x] 双击节点改名 → MD 同步；MD 编辑 → 导图重排（动画）
- [x] 缩放 +/−/适应窗口，下限锁 fit；滚轮向光标缩放
- [x] 节点工具栏 增子/增同级/折叠/删除；键盘快捷键
- [x] Mock 流式：`把人物线展开` → 11→14 节点 + chip
- [x] **真实模型（桥接）**：`/api/health` 识别 CLI+代理；`/api/chat` 流式改图（见 §7）
- [x] 深浅主题切换；导图主题新建/删除
- [x] 多文档/分组 增删改名、搜索、模板、自动保存
- [x] 导入 .md；导出 .md / SVG / PNG
- [x] 设置：Mock / 自定义 API（含本地探测+测试连接）/ 本地 CLI 三模式

逻辑单测（`mdToTree`/`treeToMd` 往返、`DocStream` 跨 chunk 协议分流）与 DOM 初始化冒烟脚本见 `/tmp` 调试产物，可按需重建。

---

## 7. 真实模型端到端验证（示例）
```bash
# 1) 启动桥接
node server.js &
# 2) 识别探测
curl -s http://127.0.0.1:4317/api/health | jq '{cli,cliVersion,proxyOk,model,ready}'
# 3) 直接打一次流式（应看到 data: {"text":"..."} 逐条）
curl -s -X POST http://127.0.0.1:4317/api/chat -H 'content-type: application/json' \
  -d '{"messages":[{"role":"system","content":"只输出最终答案。"},{"role":"user","content":"1+1=?"}]}'
# 4) 浏览器打开 http://127.0.0.1:4317，确认芯片为「本地 Claude · <model>」，发送改图指令观察生长
```

---

## 8. 部署

- **本机自用**：`node server.js` 即可；或仅 file:// 用 Mock/云端 API。
- **静态托管**（仅 Mock + 云端 API，无桥接）：`mindweave.html` 单文件丢到 Cloudflare Pages / Vercel / 任意静态主机。注意：此模式**无本地 CLI 识别**（识别依赖本机 server.js）。
- **不要**把含 Key 的配置提交到仓库；桥接不存 Key，云端 API 的 Key 仅存于使用者浏览器 localStorage，**仅适合自用**。

---

## 9. 安全

- 仓库与代码**不含任何密钥**；`server.js` 运行时从用户本机 `~/.claude/settings.json` 读取并原样转发，不落盘、不上报。
- 桥接仅监听 `127.0.0.1`，不对外暴露。
- 纯前端云端模式的 Key 明文存 localStorage，自用可接受；对外发布需加后端中转。

---

## 10. 已知限制

- 后台标签页 Mock 流式被计时器节流（真实模型路径不受影响）。
- 上游代理若更换鉴权方式，需同步 `server.js` 的请求头。
- 单次改图携带全文，超长文档请分块梳理。
- 数据仅本地浏览器，换设备/清缓存会丢，请导出备份。
