# MindMap AI 对话网页 — 工程规划文档 v3

> 项目目标：一个自用/内测网页应用。用户与 AI 对话梳理思路，AI 生成/修改 Markdown，思维导图与 Markdown **双向实时同步**；AI 流式输出时导图**逐节点生长**；本地保存多个文档；支持自定义第三方 API 与本地 LLM。
> 本文档面向执行开发的 Agent/工程师。

---

## 〇、需求决策记录（与需求方确认，勿再变更）

| 决策点 | 结论 |
|--------|------|
| 用户范围 | 自用/内测 → 纯前端，API 配置用户自填存 localStorage，无后端 |
| AI 角色 | 双角色：能聊天也能改图 → 「`[[DOC]]` 输出协议」统一（见 3.2） |
| 导图刷新 | AI 流式输出时导图逐节点生长（见 3.3） |
| 文档保存 | 本地多文档：IndexedDB + 左侧栏分组管理 + 自动保存 |
| 使用场景 | 头脑风暴、读书笔记、小说大纲等 → 模板预设（见 4.6） |
| **界面主题** | **深色 + 浅色双主题，带开关**；注意文字/背景对比度（见 3.5） |
| **整体布局** | 左侧栏（设置/工具/分组主题列表）｜中间导图主体｜右栏（上 MD 编辑器 + 下 AI 对话）；**三栏均可收缩**（见 3.6） |
| **Mock 模式** | 演示数据用**小说大纲**形式 |
| **API 接入** | 预设主流服务 + **自定义第三方 API（OpenAI 兼容端点）** + **本地 LLM（Ollama / LM Studio 等）**（见 3.7） |
| **导图缩放** | 默认适配窗口；放大可到单主题占满窗口；**缩小不低于适配窗口大小**（见 3.8） |

---

## 一、产品功能（MVP）

| # | 功能 | 说明 |
|---|------|------|
| F1 | AI 对话面板 | 完整聊天体验：可纯讨论也可下指令改图；流式输出；位于右栏下方 |
| F2 | Markdown 编辑器 | 可编辑，实时反映到导图；位于右栏上方 |
| F3 | 思维导图画布 | 页面中央主体；展开/折叠、拖拽、双击编辑节点；图上改动实时回写 MD |
| F4 | 三方实时同步 | 对话 / MD / 导图任一变化其余同步（单一数据源） |
| F5 | 逐节点生长动画 | AI 输出 MD 过程中导图按行增量生长 |
| F6 | 多文档管理 | 左侧栏**分组**文档列表：分组（头脑风暴/读书/小说/自定义）、新建（选模板）、重命名、删除、切换；自动保存 |
| F7 | 场景模板 | 空白、头脑风暴、读书笔记、小说大纲（初始 MD + system prompt 预设） |
| F8 | 主题切换 | 深色/浅色开关，全站（含导图配色）跟随 |
| F9 | 可收缩布局 | 左栏、右栏可折叠隐藏，导图区自适应撑满；右栏上下两块可拖拽调比例 |
| F10 | 多源 LLM 接入 | 预设（Moonshot/DeepSeek/OpenAI）+ 自定义第三方端点 + 本地 LLM |
| F11 | 导图缩放控制 | 缩放按钮 + 滚轮缩放 + 一键适应窗口；缩放范围按 3.8 约束 |
| F12 | 导入/导出 | 导出 .md、导图导出 PNG/SVG；导入 .md |

**非 MVP（二期）**：账号/云同步（届时加后端）、协作编辑、历史版本快照。

---

## 二、技术选型（已定）

| 层 | 选型 | 说明 |
|----|------|------|
| 框架 | React 18 + TypeScript + Vite | — |
| UI | Tailwind CSS + shadcn/ui + `react-resizable-panels` | 双主题用 Tailwind `dark:` class 方案；可收缩面板用 resizable-panels 的 collapsible 能力 |
| 思维导图 | **simple-mind-map** | 原生 MD 导入/导出；支持自定义主题配色（深浅两套）、缩放控制 API（`view.zoomIn/zoomOut/zoomFit/setTransform`） |
| MD 编辑器 | CodeMirror 6 | markdown 高亮；深浅主题（`@codemirror/theme-one-dark` 或自配） |
| 状态 | Zustand | 单一数据源 store |
| 本地存储 | Dexie（IndexedDB） | 文档 + 分组 + 应用设置 |
| LLM 客户端 | `openai` npm 包 | baseURL/apiKey/model 全可配，天然覆盖第三方与本地端点 |
| 流式解析 | 自研轻量行级解析器 | 见 3.3 |

**必读参考源码**：github.com/wanglin2/mind-map（重点：`parse/markdown.js`、主题配置项、view 缩放 API）；github.com/markmap/markmap（视觉参考）。

---

## 三、核心架构与关键设计

### 3.1 单一数据源

```
┌────────────────────────────────────────────────────────────┐
│ Zustand Store                                              │
│  currentDocId, markdownText, lastSource(editor|canvas|ai), │
│  theme(light|dark), llmConfig, panelLayout                 │
└────▲───────────────┬────────────────┬──────────────▲───────┘
     │ write         │ subscribe      │ subscribe    │ write(流式)
┌────┴───────┐ ┌─────┴──────┐ ┌────────┴────────────┐
│ MD 编辑器   │ │ 导图画布    │ │ AI 对话面板          │
└────────────┘ └────────────┘ └─────────────────────┘
```

- Markdown 文本是唯一数据源；导图是投影。
- 写入带 source 标记，订阅方忽略自身触发的更新，防循环。
- 编辑器写入防抖 300ms；外部更新时保留编辑器光标。

### 3.2 AI 输出协议（双角色统一方案）

```
[正常的聊天回复文本……]
（仅当需要修改文档时，另起一行：）
[[DOC]]
# 完整的新 Markdown 文档……
```

- 前端流式解析：`[[DOC]]` 之前 → 聊天气泡；之后 → 流式 MD 管线驱动导图生长。
- 闲聊不含 `[[DOC]]`，导图不动。

**System Prompt 草案**：
```
你是思维梳理助手，页面上与用户对话，旁边是一份用 Markdown（# 标题 + - 列表层级）
表达的思维导图文档。
规则：
1. 正常用中文与用户讨论；当讨论产生了应沉淀到文档的内容，或用户要求修改文档时，
   在回复末尾另起一行输出 [[DOC]]，随后输出修改后的完整 Markdown。
2. [[DOC]] 之后只输出 Markdown 本身，不要代码块标记、不要解释。
3. 未提及的既有内容保持原样；层级最多不超过 5 层。
4. 不需要修改文档时，不要输出 [[DOC]]。
```

### 3.3 流式逐节点生长

```
LLM 流式 token → 累积 buffer → 按行切分
  → 每凑齐一行完整结构行（# 标题 或 - 列表项）→ 节点事件 → 更新导图
  → 兜底：300ms 节流整体 setData，视觉仍接近"生长"
```

- 行级正则解析：`^(#{1,5})\s+(.+)`、`^(\s*)-\s+(.+)`；遇 `\n` 才提交该行。
- **先实现节流整体刷新，增量解析作为增强项**，勿过度工程。
- 解析器要容错：模型可能带代码块包裹、多余空行，需清洗。

### 3.4 本地多文档与分组

- Dexie 表：`docs(id, groupId, title, markdown, template, createdAt, updatedAt)`、`groups(id, name, order)`、`settings(key, value)`
- 内置默认分组：头脑风暴 / 读书笔记 / 小说 / 未分组；用户可增删分组。
- store 变更防抖 1s 自动保存；左侧栏按分组展示、组内按 updatedAt 排序。

### 3.5 双主题（深色/浅色）

- 开关在左侧栏底部（太阳/月亮图标）；选择持久化到 settings；默认跟随系统 `prefers-color-scheme`。
- Tailwind `dark` class 策略，shadcn/ui 自带双套 CSS 变量。
- **对比度要求**：正文文字与背景对比度 ≥ 4.5:1（WCAG AA）；次级文字 ≥ 3:1。
- **导图配色需单独处理**：simple-mind-map 的节点颜色、连线、背景不跟随 CSS 变量，需准备深浅两套导图主题配置，切换主题时调用其 `setTheme`/自定义主题 API 同步切换；CodeMirror 同理切换明暗主题。
- 浅色：白底深灰字；深色：建议 `#0f1115` 级深灰底（避免纯黑刺眼）+ `#e6e6e6` 级文字，强调色统一一个品牌色（建议靛蓝/青色系，深浅模式下分别调亮度）。

### 3.6 布局（三栏可收缩）

```
┌──────────┬────────────────────────┬──────────────┐
│ 左侧栏    │                        │ 右栏(上)      │
│ · 新建    │                        │ MD 编辑器     │
│ · 分组    │      思维导图           ├──────────────┤
│   文档列表│      （中央主体）       │ 右栏(下)      │
│ · 工具    │                        │ AI 对话       │
│ · 设置    │                        │              │
│ · 主题开关│                        │              │
└──────────┴────────────────────────┴──────────────┘
```

- 参考主流对话产品的排布习惯（左导航、中央内容、右辅助）。
- 左栏、右栏均可点击边缘按钮**折叠/展开**（`react-resizable-panels` 的 collapsible panel）；折叠后导图区自适应撑满。
- 右栏内部上下两块（MD / 对话）可拖拽调比例，对话区也可单独最大化。
- 比例与折叠状态持久化到 settings。
- 导图区顶部浮动工具条：放大 / 缩小 / 适应窗口 / 导出 PNG / SVG。

### 3.7 LLM 接入（多源）

统一抽象：**任何 OpenAI 兼容端点** = baseURL + apiKey + model。本地 LLM 只是 baseURL 指向 localhost 的特例。

设置页提供三档：

| 档位 | 内容 |
|------|------|
| 预设服务 | 下拉选 Moonshot(`https://api.moonshot.cn/v1`) / DeepSeek(`https://api.deepseek.com/v1`) / OpenAI(`https://api.openai.com/v1`)，自动填 baseURL，用户只填 Key 和选模型 |
| 自定义第三方 | 手动填 baseURL + apiKey + model 三项（兼容任意 OpenAI 兼容服务，如通义、智谱、OpenRouter 等） |
| 本地 LLM | 快捷按钮填 `http://localhost:11434/v1`（Ollama）或 `http://localhost:1234/v1`（LM Studio）；apiKey 可填任意非空字符串；model 手填本地模型名 |

- 多组配置可保存为「配置档案」切换（存 settings）。
- **本地 LLM 注意事项（写进设置页提示）**：
  - Ollama 需设置 `OLLAMA_ORIGINS` 允许浏览器跨域，LM Studio 需在设置里开启 CORS；
  - 本地模型指令遵循能力弱，`[[DOC]]` 协议可能不稳定 → 检测到本地端点时在 UI 提示「建议使用 7B 以上的 instruct 模型」。
- 连接测试按钮：发一条 1-token 请求验证配置可用。
- 无有效配置 → mock 模式（演示数据为**小说大纲**：AI 与用户讨论一部侦探小说，逐轮往大纲里加章节/人物分支）。

### 3.8 导图缩放规则

- **默认/下限**：`zoomFit` 适应窗口大小——不允许缩得比「整图适配窗口」更小（缩小按钮在到达 fit 时置灰，滚轮同约束）。
- **上限**：放大到「单个主题节点占满可视窗口」的级别。实现上换算：以节点字号为基准，限制 scale 不超过使单节点文本接近窗口高度可读放大的倍数（工程上可先设 `maxScale ≈ 4`，再按视觉微调）。
- 每次导图数据变化（生长/同步）后保持当前缩放比例与中心，**不强制重置**；仅用户点「适应窗口」时才 zoomFit。
- 窗口 resize / 面板折叠展开时自动重新 zoomFit（仅在当前缩放等于 fit 时跟随；用户手动放大过则不打扰）。
- simple-mind-map 对应 API：`view.zoomIn() / zoomOut() / zoomFit() / setTransform()`，缩放范围可在其实例配置或调用层钳制。

---

## 四、模块拆解与开发任务

| 模块 | 文件建议 | 任务要点 |
|------|----------|----------|
| Store | `src/store/useDocStore.ts` | 文档/分组/主题/LLM配置/面板布局状态；source 标记 |
| 导图画布 | `src/components/MindMapCanvas.tsx` | 初始化与销毁；深浅主题切换；缩放钳制(3.8)；`data_change` 回写 MD；顶部浮动工具条 |
| MD 编辑器 | `src/components/MdEditor.tsx` | CodeMirror 6；明暗主题；防抖写入；保光标 |
| 对话面板 | `src/components/ChatPanel.tsx` | 消息列表、流式气泡、协议分流(3.2) |
| 流式解析器 | `src/lib/streamParser.ts` | 协议分流 + 行级 MD 事件 + 清洗容错 |
| LLM 客户端 | `src/lib/llm.ts` | OpenAI 兼容流式；多配置档案；连接测试；mock 模式（小说大纲演示数据 `src/lib/mockNovel.ts`） |
| 持久化 | `src/lib/db.ts` | Dexie：docs/groups/settings 三表 + 自动保存 hook |
| 左侧栏 | `src/components/AppSidebar.tsx` | 新建(选模板)、分组管理、文档列表、设置入口、主题开关 |
| 右栏 | `src/components/RightPanel.tsx` | 上 MD 编辑器 + 下对话，可拖拽调比例 |
| 布局 | `src/App.tsx` | resizable-panels 三栏 + 折叠状态持久化 |
| 场景模板 | `src/lib/templates.ts` | 见 4.6 |
| 设置弹窗 | `src/components/SettingsDialog.tsx` | 3.7 三档接入 + 配置档案管理 |
| 导入导出 | `src/lib/io.ts` | .md 导入导出；PNG/SVG 导出 |

**依赖清单**：`simple-mind-map`、`zustand`、`dexie`、`@uiw/react-codemirror`、`@codemirror/lang-markdown`、`openai`、`react-resizable-panels`、`lucide-react`

### 4.6 场景模板预设（templates.ts）

| 模板 | 初始 MD 骨架 | system prompt 侧重 | 默认分组 |
|------|-------------|-------------------|----------|
| 空白 | `# 中心主题` | 通用 | 未分组 |
| 头脑风暴 | 分支：目标/想法/风险/下一步 | 鼓励发散、主动追问、及时沉淀进 [[DOC]] | 头脑风暴 |
| 读书笔记 | 分支：核心观点/章节脉络/金句/我的思考 | 引导按章节拆解 | 读书笔记 |
| 小说大纲 | 分支：世界观/人物线/章节大纲/伏笔 | 结构化梳理人物关系与章节钩子 | 小说 |

---

## 五、开发流程（建议顺序）

1. **脚手架**：Vite + React + TS + Tailwind + shadcn/ui（配置 dark class 策略）
2. **M1 双向同步骨架**：store + MD 编辑器 + 导图画布，跑通「改字出图、改图出字」。*通过 = 技术风险清零*
3. **M2 布局与主题**：三栏可收缩布局、深浅双主题（含导图/CodeMirror 配色联动）、缩放钳制
4. **M3 多文档**：Dexie、分组侧栏、四套模板
5. **M4 对话 + mock AI**：聊天 UI + mock 小说大纲演示流式输出，跑通生长动画
6. **M5 真实 LLM**：三档接入设置页 + 连接测试 + 错误处理（Key 失效/限流/本地服务未启动）
7. **M6 导入导出 + 打磨**：文件 IO、空状态、加载态、对比度走查
8. **M7 构建部署**：`npm run build` → 静态托管

每个里程碑验收后再进下一个。

---

## 六、需要准备 / 注册 / 付费的清单

### 必须项（MVP，三选一或都用）

| 项目 | 费用 | 操作 |
|------|------|------|
| **Kimi / Moonshot API**（推荐） | 注册有赠送额度，约 ¥12/百万 tokens 起，自用预计几元到几十元 | platform.moonshot.cn 注册 → 充值 → 创建 API Key |
| **DeepSeek API**（备选，最便宜） | 输入约 ¥1-2/百万 tokens | platform.deepseek.com 注册充值 |
| **本地 LLM**（零 API 成本） | 免费，但吃本机算力 | 装 Ollama（ollama.com）或 LM Studio，拉一个 7B+ instruct 模型；按 3.7 开启 CORS |
| GitHub 账号 | 免费 | 建私有仓库托管代码 |

> 注：纯自用可以**只接本地 LLM，一分钱不花**；追求 [[DOC]] 协议稳定性建议至少备一个云端 Key。

### 可选项

| 项目 | 何时需要 | 费用 |
|------|----------|------|
| 静态托管 | 想在手机/他设备访问 | Cloudflare Pages / Vercel 免费档 |
| 域名 | 想要自己的网址 | ¥50-100/年 |
| ICP 备案 | 域名解析到国内服务器才需要 | 免费，约 2-4 周；海外托管可跳过 |
| 后端+数据库 | 二期做账号/云同步 | Supabase 免费档起 |

### 不需要付费

- 全部开源库（MIT/免费协议，可商用）；Node.js 20+、VS Code

---

## 七、风险与注意事项

1. **API Key 安全**：纯前端方案 Key 存 localStorage 明文——仅适合自用/内测，对外发布前必须加后端中转。
2. **循环更新**：三端同步必须 source 标记 + 防抖。最大工程细节。
3. **流式生长稳定性**：模型输出可能不规则，解析器要容错；先做节流整体刷新兜底，勿过度工程。
4. **本地 LLM 协议遵循弱**：小模型可能不遵守 [[DOC]] 格式 → 解析器容错 + UI 提示（见 3.7）。
5. **双主题遗漏点**：导图与 CodeMirror 配色独立于 CSS 变量，是最容易被遗忘的联动项；验收必须走查。
6. **缩放钳制**：下限锁 zoomFit 是非默认行为，需在滚轮/按钮/触摸板 pinch 三条路径上统一钳制。
7. **大图性能**：>500 节点需增量更新；MVP 不做。
8. **token 成本**：每次改图携带全文，文档 8000 字软限制。
9. **数据只在本地浏览器**：换设备/清数据会丢，UI 提示 + 鼓励导出备份。

---

## 八、验收标准（MVP）

- [ ] MD 输入 `# 主题` + 两级列表，导图 300ms 内正确渲染
- [ ] 导图双击节点改名，MD 对应行同步且不打断编辑器光标
- [ ] 对话闲聊导图不动；说「把第二点展开成三个子项」，气泡出回复、导图同步长出新分支
- [ ] mock 模式（小说大纲演示）流式输出时导图逐节点生长（或节流近似生长）
- [ ] 深/浅主题切换：全站含导图节点、连线、MD 编辑器同步换色，文字背景对比度清晰无阅读障碍
- [ ] 左右栏可折叠，折叠后导图自适应撑满；右栏上下比例可拖拽；刷新后布局状态保留
- [ ] 缩放：默认适配窗口；缩小到 fit 后缩不动；可放大到单主题占满窗口；生长过程缩放不被重置
- [ ] 设置页三档接入均可保存与切换；连接测试通过/失败有明确反馈；本地 Ollama 端点可正常对话（模型能力允许范围内）
- [ ] 分组新建文档、切换自动保存、刷新不丢
- [ ] 导出 .md 与编辑器一致，重新导入可还原；导图可导出 PNG/SVG
