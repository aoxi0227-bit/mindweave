# 思脉 MindWeave · 跨平台说明（Windows / macOS / Linux）

> 单文件网页 + 本地桥接后台，打包在一起。网页负责思维导图/Markdown/AI 对话，后台负责识别你本机的 **Claude CLI** 并把真实模型接到网页上——**不用填 API Key、不用 mock**。

## 它包含什么
| 文件 | 作用 |
|------|------|
| `mindweave.html` | 单文件前端（思维导图 + Markdown + AI 对话，三向同步） |
| `server.js` | 本地桥接后台（Node 标准库，**无需 npm install**） |
| `start.sh` | macOS / Linux 启动器（双击或终端运行） |
| `start.bat` | Windows 启动器（双击） |
| `start.ps1` | Windows PowerShell 启动器（备用） |
| `build-macos-app.sh` | 在 macOS 上把启动器打包成 `思脉MindWeave.app` |

## 前置条件
1. **Node.js ≥ 18**：nodejs.org 或 `brew install node` / `winget install OpenJS.NodeJS` / 包管理器安装。
   - 仅用内置 Mock 演示时，连 Node 都不需要，直接打开 `mindweave.html` 即可。
2. **想用真实模型**：本机已安装并登录 **Claude Code（`claude` CLI）**。后台会自动读取它的配置（`~/.claude/settings.json` 里的 `ANTHROPIC_BASE_URL` / 鉴权）作为上游，无需你粘贴任何 Key。
   - 没有 Claude CLI 也没关系：打开网页后仍是 Mock 演示，或在「设置」里填任意 OpenAI 兼容端点。

## 一键启动

### macOS
- 双击 **`start.sh`**；若提示无法打开：右键 → 打开 → 确认，或终端执行 `chmod +x start.sh && ./start.sh`。
- 想做成 Dock 里的 App：终端执行 `./build-macos-app.sh`，生成 `思脉MindWeave.app`，之后双击它即可（无终端窗口）。

### Linux
- 终端：`chmod +x start.sh && ./start.sh`（会自动用 `xdg-open` 打开浏览器）。

### Windows
- 双击 **`start.bat`**。若被 SmartScreen 拦截：仍要运行 → 运行。
- 或用 PowerShell：`右键 start.ps1 → 使用 PowerShell 运行`（首次可能需 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`）。
- 注意：`start.sh` 在 **Git Bash / WSL** 下也能用。

启动后浏览器自动打开 `http://127.0.0.1:4317`。后台只监听本机回环地址，不对外暴露。

## 关页即停
网页与后台用「心跳 + 客户端计数」联动：**关闭最后一个标签页 → 后台自动退出**；崩溃/强退由心跳超时兜底。想手动停：关掉终端窗口，或 `Ctrl+C`。

## 本地桥接怎么工作
- 后台 `GET /api/health`：探测 `claude` 二进制+版本，读 `~/.claude/settings.json` 的 `ANTHROPIC_BASE_URL`，请求 `/v1/models` 取模型 → 网页据此显示「本地 Claude · <model>」。
- 后台 `POST /api/chat`：把对话转成 Anthropic Messages 流式请求发给上游，SSE 实时回传给网页，网页解析 `[[DOC]]` 协议让导图逐节点生长。
- 网页与后台**同源**，无 CORS 问题；鉴权复用 CLI 配置，**仓库与发布包不含任何密钥**。

## 键盘速查
- `Tab` 新建子主题、`Enter` 新建同级主题（新建后只选中、不弹编辑框）。
- 选中节点后**直接打字**即在该节点编辑（兼容中文输入法）；`Backspace`/`Delete` 删除（可一路删到中心主题，不穿透到 Markdown）。
- `⌘Z`/`Ctrl+Z` 撤销、`⌘⇧Z`/`Ctrl+Y` 重做；方向键导航；`Space` 折叠；双击改名。
- `Tab` 新建时视口自动居中到新节点。

## 打包成 zip / 自己重新打包
本发布包即 zip 解压结果。若要重新打包：把上表文件（不含 `dist/`、`*.zip`、`.server.*`、`思脉MindWeave.app/`、`demo/`）打进 zip 即可；macOS 的 `.app` 用 `build-macos-app.sh` 现做（含当前架构的通用二进制，跨架构请在目标机器重做）。

## 排查
- 启动报「未找到 Node.js」：先装 Node。
- 芯片显示「Mock 演示」而非「本地 Claude」：说明没探测到 CLI/代理——确认 `claude` 已登录，或点「设置 → 重新识别」；也可直接用 Mock 或填云端端点。
- macOS 双击无反应：看 `/tmp/mindweave-launcher.log`；或终端跑 `./start.sh` 看报错。
- 端口被占用：`PORT=5050 ./start.sh`（Windows：`set PORT=5050 && start.bat`）。
