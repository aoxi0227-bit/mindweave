# 思脉 MindWeave · 技术文档

> 人与 AI 双向写作的「思维导图 + Markdown」单页应用，配套本地桥接服务，直接接入本机 Claude CLI。
> 本文描述**系统如何实现**（架构、算法、协议、数据流）。工程落地见《工程文档.md》。

---

## 1. 系统总览

应用由两层组成：**纯前端单页**（`mindweave.html`，零构建、零依赖）与**可选的本地桥接服务**（`server.js`，Node 标准库，无第三方包）。前端负责编辑、导图渲染与同步；桥接负责把本机的 Claude CLI / 其代理暴露为同源 HTTP 接口，从而让浏览器无需填写任何 Key 即可调用真实模型。

```
┌──────────────────────────────────────────────────────────────────┐
│  浏览器（mindweave.html，单文件）                                   │
│  ┌────────────┐  ┌──────────────────────┐  ┌──────────────────┐  │
│  │ 左侧栏      │  │  思维导图 SVG 画布     │  │ 右栏上：MD 编辑器  │  │
│  │ 文档/分组   │  │  自绘布局+动画+缩放    │  │ 右栏下：AI 对话    │  │
│  └────────────  └──────────┬───────────┘  └────────┬─────────┘  │
│                             │   单一数据源(Markdown)  │            │
│                             └───────────┬───────────┘            │
│        模式： mock │ 自定义OpenAI兼容 │ 本地Claude CLI(同源)        │
└───────────────────────────────────┬──────────────────────────────┘
                                    │ 同源 /api/health , /api/chat (SSE)
┌───────────────────────────────────▼──────────────────────────────┐
│  server.js （本地桥接，127.0.0.1:4317）                            │
│   · 识别 claude CLI 二进制 + 版本                                   │
│   · 读取 ~/.claude/settings.json → ANTHROPIC_BASE_URL / 鉴权        │
│   · /v1/models 探测可用模型  · /v1/messages 流式转发为 SSE           │
└───────────────────────────────────┬──────────────────────────────┘
                                    │ Anthropic Messages API (SSE)
                          ┌─────────▼──────────┐
                          │ 本地代理 127.0.0.1   │ → 真实模型
                          └────────────────────┘
```

设计原则：**Markdown 文本是唯一数据源**，导图是它的投影，AI 是它的协作者。三处视图（编辑器 / 画布 / 对话）通过带 `source` 标记的单一 store 同步，订阅方忽略自身触发的更新，杜绝循环。

---

## 2. 前端架构

### 2.1 三栏布局与可收缩

`#app` 为 flex 行布局：`#sidebar`（264px）｜`#stage`（flex:1）｜`#rightCol`（404px）。左右栏通过 `.closed` 类做宽度过渡折叠，折叠后画布自适应撑满；右栏内部 `#editorPane` / `#chatPane` 由 `#vSplit` 拖拽调比例（持久化 `ui.rightSplit`，双击复位 46%）。

### 2.2 状态与持久化

无框架，用模块级变量 + `localStorage`：

| Key | 内容 |
|-----|------|
| `mw.docs` | 文档数组 `{id,title,markdown,group,template,chat[],createdAt,updatedAt}` |
| `mw.groups` | 分组数组 |
| `mw.settings` | `{mode: mock|api|cli, baseURL, apiKey, model}` |
| `mw.ui` | 主题、面板开关、右栏比例、地图主题、`modeExplicit` 等 |
| `mw.themes` | 用户自建导图主题 |

写入防抖 700ms 自动保存；顶栏显示「正在编辑… / 已保存 · HH:MM」。

### 2.3 Markdown ↔ 树

`mdToTree(md)` 逐行解析：`# 标题` 设定根/层级，`- 列表` 按缩进（2 空格/级）嵌套，层级钳制 1–4。节点 id 用路径稳定编码（根 `r`，子 `r.0`、`r.0.1`…），保证重解析后选区/折叠可对应。`treeToMd(tree)` 反向序列化为 `# 根` + 缩进列表，**往返稳定**。

### 2.4 自绘思维导图引擎

不引入第三方导图库，手写 SVG 渲染，便于深度定制主题与生长动画。

- **测量**：离屏 canvas `measureText` 按深度字号折行，得节点 `w/h`。
- **布局**（右向逻辑结构）：后序求子树高 `subH`，前序放置 `place`：`x = 父.x + 父.w + HGAP`，子树垂直居中。一级节点的分支色索引 `_idx` 在布局前统一分配，保证连线着色正确。
- **渲染**：节点 `<g class="node">`，根=实底衬线卡片、一级=带色条卡片、更深=色点+文本；连线为三次贝塞尔 `M x1 y1 C … x2 y2`，取分支色、按深度变细。
- **动画**：维护 `posMap` 当前坐标，新布局用 380ms 缓动 tween 每帧重绘节点 transform 与连线 `d`，实现平滑位移；**新增节点从父节点坐标长出**（`pop` 关键帧），新增连线 `stroke-dashoffset` 绘制——流式生长即由此呈现。
- **缩放/平移**：`#gViewport` 的 `translate+scale`；滚轮向光标缩放，钳制在 `[fitScale, 4]`（**下限=适应窗口**，符合规划 3.8）；适应窗口按钮重置 `userZoomed`。
- **交互**：单击选中→浮动工具栏（子主题/同级/折叠/删除）；双击行内改名；键盘 `Tab/Enter/Space/方向键/Delete`；折叠徽标显示被隐藏子树节点数。

### 2.5 双向同步

```
编辑器 input ──debounce280ms──► tree=parse ──► renderMap(anim) ──► 存盘
画布改动(增/删/改/折叠) ──► treeToMd ──► editor.value=md ──► renderMap ──► 存盘
AI 流式 ──► 见 §3 ──► 同时写 editor 与 renderMap
```
编辑器写入不破坏光标（仅当 source≠editor 时才回写 value）。

### 2.6 主题体系

- **深浅主题**：CSS 变量 + `html.dark`；切换时重渲染导图（节点颜色在渲染时内联为具体色值，使 SVG/PNG 导出可直接携带颜色）。
- **导图主题**：内置 4 套分支配色，可在左上「地图主题」浮窗**新建**（5 色拾色器）/**删除**自建主题，满足「增减主题」。

---

## 3. AI 协作与 `[[DOC]]` 协议

AI 兼具「聊天」与「改图」两角色，统一用输出协议区分：

```
[正常聊天文本……]
（仅当需改文档时另起一行：）
[[DOC]]
# 完整的新 Markdown 文档……
```

`DocStream` 流式解析器处理跨 chunk 的标记：维护 8 字符尾缓冲以防 `[[DOC]]` 被切分；标记之前→聊天气泡（衬线渲染，带闪烁光标），之后→逐行提交到导图管线。每凑齐一行结构行即 `onLine(md)`，触发 `renderMap(true)`，**导图随输出逐节点生长**；流结束后整篇落盘并显示「⚡ 已更新导图 · N 个节点」chip。容错：剥离模型可能加的 ``` 代码围栏。

System Prompt（注入到 `system`）见工程文档；核心约束：未提及内容保持原样、层级≤5、`[[DOC]]` 后只输出 Markdown。

---

## 4. 模型接入三模式

### 4.1 Mock（离线演示）
内置《雾港孤灯》侦探小说脚本，按关键词路由改图（`把X展开` / 章节 / 伏笔 / 人物 / 反转），用 `setInterval` 模拟逐字流。**仅作离线兜底**；当本地桥接可用时应用自动切到 CLI 模式，不使用 mock。

### 4.2 自定义 OpenAI 兼容端点
任意 `baseURL+apiKey+model`，`fetch` + `ReadableStream` 解析 `data:` 行。预设 Moonshot/DeepSeek/OpenAI/Ollama/LM Studio；含**本地服务探测**（Ollama `:11434/api/tags`、LM Studio `:1234/v1/models`，带超时与 CORS 提示）、连接测试、Key 显隐。

### 4.3 本地 Claude CLI 桥接（本项目重点）
见 §5。前端以**同源**请求 `/api/health`（识别）与 `/api/chat`（SSE 流式），无需 Key、无 CORS、无 mock。

---

## 5. 本地桥接 server.js（识别 + 接入 Claude CLI）

### 5.1 识别（能否识别）
`GET /api/health` 做两件事：
1. **CLI 二进制**：在 `~/.local/bin`、`/opt/homebrew/bin`、`/usr/local/bin` 探测 `claude`，并 `spawn claude --version` 取版本（4s 超时）。
2. **代理连通**：读取 `~/.claude/settings.json` 的 `env.ANTHROPIC_BASE_URL`，请求其 `/v1/models`，解析可用模型列表与默认模型。

返回 `{cli, cliVersion, cliPath, proxyOk, baseUrl, models, model, ready}`。`ready = cli && proxyOk`。前端首屏调用，`ready` 时自动将模式置为 `cli` 并在模型芯片显示「本地 Claude · <model>」；设置面板「本地桥接」卡显示识别结果（CLI 版本 + 代理地址 + 模型）。

> 桥接会探测本机 `claude` 二进制与版本，并读取其 `ANTHROPIC_BASE_URL` 作为上游；模型取自上游 `/v1/models`（示例环境为本地代理 + 某模型，按你本机实际为准）。

### 5.2 接入（流式改图）
`POST /api/chat` 接收 `{messages}`（OpenAI 形态，含 system），server 拆出 `system` 与对话，向 `BASE_URL/v1/messages` 发起 **Anthropic 原生流式**请求（`stream:true`，带 `x-api-key`/`anthropic-version`），把上游 SSE 的 `content_block_delta.text_delta` 实时转写为 `data: {"text":...}`，`message_stop` 转 `{"done":true}`，错误转 `{"error":...}`；客户端断开时销毁上游。鉴权直接复用 CLI 配置中的 `ANTHROPIC_API_KEY`（若由本地代理托管则原样转发），**仓库与代码不含任何密钥**。

### 5.3 静态托管
同一 server 以同源方式提供 `mindweave.html`，故 `/api/*` 与页面同源，规避 CORS。

### 5.4 为什么用代理 HTTP 而非 `claude -p` 子进程
实测 `claude -p --output-format stream-json` 可用，但其输出经本机 hook（rtk）管道时会被过滤，且子进程冷启动慢、权限/TTY 行为不确定。代理本身就是 Claude CLI 的后端、原生支持 token 级 SSE，故桥接选 HTTP 路径以获得稳定流式；CLI 二进制仍用于「识别」信号，二者结合即「识别 + 接入本地 Claude CLI」。

---

## 6. 导出

- **Markdown**：Blob 下载。
- **SVG**：克隆当前 SVG，注入字体 `<style>`、清除动画类、按 bbox 设 `viewBox`（颜色已内联，导出即所见）。
- **PNG**：SVG→`Image`→2× canvas（填主题底色）→`toBlob`。

---

## 7. 性能与边界

- 节点 <500 时整树重布局+逐帧 tween 流畅；超大图需增量更新（非 MVP）。
- 后台标签页 `setInterval`/rAF 会被节流，故 Mock 流式在后台变慢——**真实模型路径不受此影响**（网络流式）。
- 文档约 8000 字软上限（每次改图携全文）。
- 数据仅本地浏览器；鼓励导出备份。

---

## 8. 关键文件

| 文件 | 职责 |
|------|------|
| `mindweave.html` | 前端全部（布局/引擎/同步/协议/三模式 UI） |
| `server.js` | 本地桥接（识别 + 代理流式 + 静态托管） |
| `技术文档.md` | 本文 |
| `工程文档.md` | 运行/扩展/验证/部署 |
| `思维导图AI对话网页-工程规划文档.md` | 原始需求与决策记录 |
